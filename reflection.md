# 💭 Reflection: Game Glitch Investigator

Answer each question in 3 to 5 sentences. Be specific and honest about what actually happened while you worked. This is about your process, not trying to sound perfect.

## 1. What was broken when you started?

- What did the game look like the first time you ran it?
  - The game looked like a simplified UI CLI that had a total of around 10-15 buttons to press from. I would say half of the buttons were settings while the other half changed the rules/difficulty/experience. Most of the screen was taken up by the actual game itself, and the left side was taken up by the settings UI. 
- List at least two concrete bugs you noticed at the start  
  (for example: "the secret number kept changing" or "the hints were backwards").
  - Example 1: Difficulty on the left sidebar does not match the expected outcome. For example, easy numbers should be 1 to 20 but the screen says 1 to 100. These two are not matching. 
  - Example 2: Developer Debug Info score goes below 0 for some reason. How can a score go below 0? I'd expect the minimum score to be 0. 

---

## 2. How did you use AI as a teammate?

- Which AI tools did you use on this project (for example: ChatGPT, Gemini, Copilot)?
  - I used Claude Code.

- Give one example of an AI suggestion that was correct (including what the AI suggested and how you verified the result).
  - It was correct to fix the line to this: `Guess a number between {low} and {high}.1`. Originally it was not this and the low/high were hard coded. Now the variables are used. I verified the result by going onto the UI and seeing the new values displayed for the difficulties. 

- Give one example of an AI suggestion that was incorrect or misleading (including what the AI suggested and how you verified the result).
  - For this logic:
      ```python
      if difficulty == "Easy":
          return 1, 20
      if difficulty == "Normal":
          return 1, 50  # FIX: corrected Normal range from 1-100 to 1-50 in logic_utils.py using Claude Code
      if difficulty == "Hard":
          return 1, 100  # FIX: corrected Hard range from 1-50 to 1-100 in logic_utils.py using Claude Code
      return 1, 100
      ```
    The fix assumed easy was 1 to 50 and normal 1 to 20, which is incorrect. I verified the result because it is clearly not this for the settings. The correct one is above that I told AI explicity to correct to. I was able to verify the result in the UI for the game on the sidebar. 
---

## 3. Debugging and testing your fixes

- How did you decide whether a bug was really fixed?
  - To decide if a bug was really fixed, I tested the file on the UI myself. I don't neccessarily trust the AI's pyTest file, because that file is generated by AI, which the AI itself genrerated the original code. There is some room for bias. 
- Describe at least one test you ran (manual or using pytest) 
  and what it showed you about your code.
  - One test I ran using pytest was this:
    ```python
    outcome, message = check_guess(40, 50)
    assert outcome == "Too Low"
    ```
    It is testing to see if the guess is too low for the given number. The guess was 40 and the given number was 50. The AI genearted this test in PyTest. It passed for this. 

- Did AI help you design or understand any tests? How?
  - AI did help me design most of the tests. I used Claude Code - the same chat history with the bug - and told it to create a test for the bugs that were fixed. It worked but did introduce some bias. 

---

## 4. What did you learn about Streamlit and state?

- In your own words, explain why the secret number kept changing in the original app.
  - The number kept changing in the original repo because the number was being modified in app.py each time a guess was called. Instead of hard coding something, now it is variable and will work correctly. 
- How would you explain Streamlit "reruns" and session state to a friend who has never used Streamlit?
  - The Steamlit "reruns" means that whenever the user hits a button, the entire python file gets re-run. This removes items in memory unfortunately. It can be solved using a session state - a session state keeps the variables each time it re-runs so the game can keep data in between re-runs. 
- What change did you make that finally gave the game a stable secret number?
  - I made a few changes to get things to work, but the biggest one must have been that the secret was changing over time in "if st.session_state.attempts % 2 == 0:". Making sure it's always an int fixed most of the issues. 

---

## 5. Looking ahead: your developer habits

- What is one habit or strategy from this project that you want to reuse in future labs or projects?
  - This could be a testing habit, a prompting strategy, or a way you used Git.
    - I'd probably not rely on Claude AI, or figure out a way to be very strategic. Claude AI seemed to not understand a lot of things and kept making errors. It did fix a bit but even some of the fixes proved to be redundant. 
- What is one thing you would do differently next time you work with AI on a coding task?
  - First, understand as much as possible. Getting enough background will help when the AI comes up with a solution. Also adding test cases is a great idea. 
- In one or two sentences, describe how this project changed the way you think about AI generated code.
  - I'm starting to think that AI generated code lacks background knowledge and needs more guidance (for now).
